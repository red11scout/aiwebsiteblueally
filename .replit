run = "node dist/index.js"
entrypoint = "server/index.ts"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "NODE_ENV=production node dist/index.js 2>&1"]
build = ["sh", "-c", "npm install && npm run build"]
